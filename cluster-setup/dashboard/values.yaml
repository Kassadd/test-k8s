kubernetes-dashboard:
    ingress: 
        enabled: true
        hosts: 
        - dashboard.javirp.com
        annotations:
            kubernetes.io/ingress.class: nginx-public
    
    enableInsecureLogin: true

    rbac:
        clusterAdminRole: true